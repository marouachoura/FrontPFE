apiVersion: v1
kind: Service
metadata:
  name: front-service
spec:
  type: NodePort
  selector:
    app: FrontPFE
  ports:
    - port: 4200
      targetPort: 4200
      nodePort: 31001
      protocol: TCP
